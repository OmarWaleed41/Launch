<Window x:Class="Launch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="Launch"
        Height="450"
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False">

    <Grid>
        <Canvas x:Name="GridCanvas" IsHitTestVisible="False"/>

        <Canvas x:Name="MainCanvas" Background="Transparent" ClipToBounds="False">
            <tb:TaskbarIcon x:Name="MyNotifyIcon"
                            IconSource="pack://application:,,,/src/imgs/gui.ico"
                            ToolTipText="Launch"
                            TrayLeftMouseUp="Settings_Click">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Settings" Click="Settings_Click"/>
                        <MenuItem Header="Refresh" Click="Refresh_Page"/>
                        <MenuItem Header="Exit" Click="Exit_Click"/>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </Canvas>
    </Grid>
</Window>
